# nodets-canil
Projeto feito no módulo do curso de node + typescript

### Pré-requisitos globais:
`npm i -g nodemom typescript ts-node`
`yarn add global nodemom typescript ts-node`

### Instalação:
`npm install`

### Para rodar o projeto
`npm run start-dev`